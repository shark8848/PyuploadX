# PyPI credentials for pyuploadx publishing
# Copy to config/pypi.env and fill in your token.
# config/pypi.env is gitignored — never commit secrets.

PYUPX_PYPI_USERNAME=__token__
PYUPX_PYPI_TOKEN=replace_with_your_pypi_api_token
PYUPX_PYPI_REPOSITORY_URL=https://upload.pypi.org/legacy/
